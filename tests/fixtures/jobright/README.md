# JobRight fixtures

Sanitized HTML/label fixtures derived from live captures may be committed here after review.

Raw captures stay in `fixtures/live-captures/` (gitignored).

Generate:

```text
npm run record:jobright -- --workflow job-feed --derive-fixtures
```
